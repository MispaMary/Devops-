
version: 2.1

Jobs:
  print_hello:
    docker:
      - image: circleci/node:13.8.0
    steps: 
      - run: return 1
      - run: 
          name: on error
          command: echo "hello error"
          when: on_fail
    
workflows:
  welcome:
    jobs:
      - print_hello

